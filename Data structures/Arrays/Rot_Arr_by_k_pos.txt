🔧 C प्रोग्राम: किसी ऐरे को K स्टेप्स से दाएँ घुमाना (Rotate Right by K Steps)

---------------------------------------------------------
🧠 समस्या:
आपके पास एक ऐरे (array) है जैसे: [1, 2, 3, 4, 5, 6, 7]
अब आपको इसे K = 3 स्टेप्स से दाईं ओर घुमाना है।

👉 परिणाम:
[5, 6, 7, 1, 2, 3, 4]
---------------------------------------------------------

👣 हल: रिवर्सिंग मेथड (Reversal Algorithm)

1️⃣ पहले पूरा ऐरे उल्टा करो (reverse करो)
2️⃣ पहले K एलिमेंट्स को उल्टा करो
3️⃣ बाकी बचे (n-K) एलिमेंट्स को उल्टा करो

---------------------------------------------------------
🧮 कोड:
#include <stdio.h>

// किसी हिस्से को उल्टा करने वाला फ़ंक्शन
void reverse(int* arr, int start, int end) {
    while (start < end) {
        int temp = arr[start];
        arr[start] = arr[end];
        arr[end] = temp;
        start++;
        end--;
    }
}

// मुख्य रोटेट फ़ंक्शन
void rotate(int* nums, int n, int k) {
    k = k % n;  // यदि k बहुत बड़ा है तो इसे n के अंदर लाओ

    reverse(nums, 0, n - 1);       // पूरा ऐरे उल्टा करो
    reverse(nums, 0, k - 1);       // पहले K एलिमेंट्स उल्टा करो
    reverse(nums, k, n - 1);       // बाकी बचे एलिमेंट्स उल्टा करो
}

// ऐरे प्रिंट करने वाला फ़ंक्शन
void printArray(int* arr, int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

// मुख्य कार्यक्रम
int main() {
    int nums[] = {1, 2, 3, 4, 5, 6, 7};
    int n = sizeof(nums) / sizeof(nums[0]);
    int k = 3;

    printf("मूल ऐरे:\n");
    printArray(nums, n);

    rotate(nums, n, k);

    printf("%d स्टेप्स से दाईं ओर घुमाने के बाद ऐरे:\n", k);
    printArray(nums, n);

    return 0;
}

---------------------------------------------------------
🧠 "k = k % n" क्यों किया?
अगर k, n से बड़ा है (जैसे 100000), तो वही रोटेशन बार-बार होता है।
इसलिए हम k को n के अंदर लाने के लिए k % n करते हैं।

उदाहरण:
k = 6, n = 5 => 6 % 5 = 1
तो 6 स्टेप्स की रोटेशन = 1 स्टेप्स की रोटेशन

---------------------------------------------------------
✅ निष्कर्ष:
- यह समाधान इन-प्लेस (in-place) है
- टाइम: O(n)
- स्पेस: O(1)
- इंटरव्यू में यही तरीका पसंद किया जाता है

---------------------------------------------------------
